[project]
name = "fraud-detection-api"
version = "2.5.0"
requires-python = ">=3.9"
dependencies = [
    "fastapi>=0.100.0",
    "uvicorn>=0.20.0",
    "pydantic>=2.0.0"
]

[tool.vercel]
entrypoint = "server:app"
